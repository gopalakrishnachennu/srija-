# SQLite works immediately for the demo.
DATABASE_URL=sqlite:///test_runner.db
API_URL=http://127.0.0.1:5000

# For PostgreSQL, use a URL like this instead:
# DATABASE_URL=postgresql+psycopg://postgres:password@localhost/test_runner

